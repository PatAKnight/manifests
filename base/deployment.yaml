apiVersion: apps/v1
kind: Deployment
metadata:
  name: backstage
  namespace: "backstage"
  labels: 
    app.kubernetes.io/component: backstage
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/component: backstage
  template:
    metadata:
      labels:
        app.kubernetes.io/component: backstage
      annotations:
        checksum/app-config: 44136fa355b3678a1146ad16f7e8649e94fb4fc21fe77e8310c060f61caaff8a
    spec:
      serviceAccountName: default
      containers:
        - resources:
            limits:
              cpu: 300m
            requests:
              cpu: 200m
          name: backstage-showcase
          image: quay.io/rh_ee_pknight/backstage-showcase:key
          imagePullPolicy: "Always"
          command:
            - node
            - packages/backend
          env:
            - name: APP_CONFIG_backend_listen_port
              valueFrom:
                secretKeyRef:
                  key: APP_CONFIG_backend_listen_port
                  name: values-app-config
          ports:
            - name: backend
              containerPort: 7007
              protocol: TCP
        - resources:
            limits:
              cpu: 300m
            requests:
              cpu: 200m
          args:
          - --provider=oidc
          - --email-domain=*
          - --upstream=http://localhost:7007
          - --http-address=0.0.0.0:4180
          - --skip-provider-button
          env:
          - name: OAUTH2_PROXY_CLIENT_ID
            valueFrom:
              secretKeyRef:
                key: OAUTH2_PROXY_CLIENT_ID
                name: oauth2-secret
          image: quay.io/oauth2-proxy/oauth2-proxy:latest
          imagePullPolicy: IfNotPresent
          name: oauth2-proxy
          ports:
          - containerPort: 4180
            name: oauth2-proxy
            protocol: TCP