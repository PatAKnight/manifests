apiVersion: v1
kind: Service
metadata:
  name: backstage
  namespace: "backstage" 
  labels:
    app.kubernetes.io/component: backstage
spec:
  type: ClusterIP
  sessionAffinity: None
  ports:
    - name: http-backend
      port: 4180
      targetPort: oauth2-proxy
      protocol: TCP
  selector:
    app.kubernetes.io/component: backstage